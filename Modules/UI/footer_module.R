# footer --------------------------------------------------------------

footer_ui <- function(id) {
  
  # namespace ---------------------------------------------------------------
  
  ns <- NS(id)
  
  # main ui ----------------------------------------------------------------------
  
  bs4DashFooter()
}

# server ------------------------------------------------------------------

footer_server <- function(input, output, session) {
  return(list(sucessfull = 1))
}
